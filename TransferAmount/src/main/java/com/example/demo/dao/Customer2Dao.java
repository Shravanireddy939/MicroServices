package com.example.demo.dao;


import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Modifying;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import org.springframework.stereotype.Repository;
import org.springframework.transaction.annotation.Transactional;

import com.example.demo.entity.WalletAccount;
import com.example.demo.entity.WalletTransactions;


 
//@Repository
//public interface Customer2Dao  extends JpaRepository<WalletTransactions, Integer> {

	/*@Query("select e from WalletTransactions e where e.WalletTransactions.accountId=?1")
	WalletTransactions find1(@Param("C")Integer accountId1);                               //to insert data in database

	@Query("select e from WalletTransactions e where e.WalletTransactions.accountId=?1")
	WalletTransactions find2(Integer accountId2);*/
	
	

		



	

	//}

